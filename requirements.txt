# --- Core web ---
Flask==3.1.0
Werkzeug==3.1.3
Flask-WTF==1.2.1
Flask-Limiter==3.12
flask-talisman==1.1.0
python-dotenv>=1.0.1

# --- PDF / imagens ---
# PyPDF2 2.x garante PdfReader/PdfWriter compatíveis com nosso editor
PyPDF2>=2.12,<3
pikepdf>=9.0,<10
pypdfium2==4.30.0
Pillow==11.1.0

# Detecta MIME real (Windows precisa do *-bin*)
python-magic>=0.4.27; sys_platform != "win32"
python-magic-bin>=0.4.14; sys_platform == "win32"

# Overlays (redação visual e texto) — usado no Editor de PDF
reportlab>=3.6,<4

# --- Tabelas / planilhas (lattice-first) ---
camelot-py==0.10.1
opencv-python-headless==4.8.1.78
ghostscript==0.8.1
pdfplumber==0.11.7
pandas==2.3.1
# Numpy 2.x exige Python >= 3.11; no nosso Docker (Py 3.10) use 1.26.4
numpy==1.26.4
openpyxl==3.1.5
tabulate==0.9.0
pdfminer.six==20250506
pdf2docx==0.5.8

# --- OCR (opcional; necessário para caixa "Aplicar OCR" do Editor) ---
# Última estável (12/set/2025). Mantemos no major 16.x por estabilidade.
ocrmypdf>=16.11.0,<17